# netlify.toml (at repo root)
[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

# SPA fallback (keeps client-side routing working)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200